# Nexus Name Service (NNS) - Environment Variables Template
# Copy this file to .env.local and fill in your values

# ============================================
# SIWE Authentication
# ============================================
# Generate a random secret key (minimum 32 characters)
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_random_secret_key_here_minimum_32_characters

# ============================================
# WalletConnect Configuration
# ============================================
# Get your Project ID from https://cloud.walletconnect.com
# 1. Create a free account
# 2. Create a new project
# 3. Copy the Project ID
NEXT_PUBLIC_WALLETCONNECT_ID=your_walletconnect_project_id_here

# ============================================
# Pinata IPFS Configuration
# ============================================
# Get API credentials from https://app.pinata.cloud
# 1. Sign up for free account
# 2. Go to API Keys → New Key
# 3. Enable Admin permissions
# 4. Copy the JWT token
PINATA_JWT=your_pinata_jwt_token_here
NEXT_PUBLIC_GATEWAY_URL=https://gateway.pinata.cloud

# ============================================
# Optional: Custom Configuration
# ============================================
# Uncomment and modify if needed

# Custom Pinata gateway (if you have dedicated gateway)
# NEXT_PUBLIC_GATEWAY_URL=https://your-custom-gateway.mypinata.cloud

# ============================================
# Development Notes
# ============================================
# - Never commit .env.local to git (it's in .gitignore)
# - SESSION_SECRET should be different for dev/staging/production
# - NEXT_PUBLIC_* variables are exposed to browser
# - Other variables are server-side only
# - Restart dev server after changing .env.local
